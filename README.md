zsteg
======


Description
-----------
zsteg升级版，增加更多的method，以发现更多隐藏的信息


Installation
------------
    gem install bundler
    bundle install
    gem build zsteg.gemspec
    sudo gem install zsteg-*.gem

